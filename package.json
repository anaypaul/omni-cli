{
  "name": "omni-cli",
  "version": "0.2.0",
  "description": "Multi-agent CLI orchestrator for Claude Code, Codex CLI, and Gemini CLI",
  "type": "module",
  "bin": {
    "omni": "./bin/omni.js"
  },
  "scripts": {
    "start": "node bin/omni.js",
    "test": "node --test 'test/*.test.js' 'test/**/*.test.js'",
    "test:e2e": "OMNI_E2E=1 node --test 'test/e2e/*.test.js'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
